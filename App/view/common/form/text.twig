{% if hasFlag %}
<div class="input-group input-group-icon">
    <span class="input-group-addon">
        <span class="icon">
            <i class="flag-icon flag-icon-{{ flag }}"></i>
        </span>
    </span>
{% endif %}

    <input type="{{ subtype }}" value="{{ value }}" class="form-control" name="{{ name }}" id="id_{{ name }}" maxlength="{{ maxlength }}"{{ readonly ? ' readonly' : '' }} />

{% if hasFlag %}
</div>
{% endif %}