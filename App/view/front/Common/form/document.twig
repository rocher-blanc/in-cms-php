<div class="text-right">
    {% for doc in docs %}
        <input type="hidden" name="{{ name }}[{{ doc.id }}]" value="{{ doc.id }}">
    {% endfor %}
    <input id="upload_{{ name }}" value="{{ image }}" type="file" class="file-loading" name="{{ name }}">
</div>