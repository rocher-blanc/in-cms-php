<style>
    .right-panel {
        position: fixed;
        top: 0;
        right: -640px;
        width: 90%;
        max-width: 640px;
        height: 100%;
        padding: 20px;
        background: white;
        z-index: 8001;
        box-shadow: 0 0 5px 0 #000a;
        overflow: auto;
        transition: all .2s;
    }
    .right-panel.open {
        transform: translateX( -100% );
        transition: all .6s;
    }
    .right-panel-loader {
        text-align: center;
        padding: 50px 0;
    }
    .right-panel-close {
        display: inline-block;
        position: absolute;
        top: 6px;
        right: 8px;
        font-size: 24px;
        background: white;
        width: 32px;
        height: 32px;
        text-align: center;
        line-height: 28px;
        border-radius: 50%;
        z-index: 1;
        border: 1px grey solid;
        cursor: pointer;
    }
</style>